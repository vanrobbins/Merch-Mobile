# v0.4 Manager Plan

**Date:** 2026-05-04  
**Branch:** `feature/v0.3`

## Execution Order

```
Agent 5 (Dashboard) ─┐
                      ├─→ Agent 7 (Simplify) ─→ Agent 8 (Tests) ─→ DONE
Agent 6 (Screens)  ─┘
```

Agents 5 and 6 are independent (different files) — run in parallel.
Agent 7 runs after both complete.
Agent 8 runs after Agent 7.

## Completion Criteria

- `flutter analyze` — zero errors
- `flutter test` — all pass
- Dashboard: no welcome banner, profile icon AppBar, store setup conditional, count pills on urgent stats, no Fixtures card
- ZoneDetailScreen: view mode has 2 action icons max (tune + proposals)
- CatalogScreen: no `_CategoryChip` class
- ProposalReviewScreen: uses MmCard
- MembersScreen: error state uses MmEmptyState
- PlanogramDetailScreen: edit-mode title has pencil icon cue

## v0.5 Deferred

See `docs/superpowers/specs/2026-04-29-v0.4-design.md` §12.
