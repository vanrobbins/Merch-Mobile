# v0.25 Interactive Layout Editor — Manager Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Orchestrate 4 agents to deliver v0.25: schema migration, bounded Store Canvas with Figma-style zone drag, type-aware fixture resize, and end-to-end planogram assignment.

**Architecture:** Agent 1 (schema) gates everything. Agents 2 and 3 run in parallel after Agent 1 merges. Agent 4 (tests) runs after both 2 and 3 merge.

**Prerequisite:** v0.2 Agents 1–5 merged to `feature/v0.2` (current working branch). Verify: `git log --oneline | head -10`.

---

## Agent Dependency Map

```
Agent 1 (Schema — gate)
    ├── Agent 2 (Store Canvas)   ─┐
    └── Agent 3 (Floor Builder)  ─┴── Agent 4 (Tests)
```

## Agent Plans

| Agent | Plan File | Status |
|---|---|---|
| Agent 1 | `docs/superpowers/plans/2026-04-27-v0.25-agent1-schema.md` | ⬜ Not started |
| Agent 2 | `docs/superpowers/plans/2026-04-27-v0.25-agent2-store-canvas.md` | ⬜ Blocked by Agent 1 |
| Agent 3 | `docs/superpowers/plans/2026-04-27-v0.25-agent3-floor-builder.md` | ⬜ Blocked by Agent 1 |
| Agent 4 | `docs/superpowers/plans/2026-04-27-v0.25-agent4-tests.md` | ⬜ Blocked by Agents 2+3 |

---

### Phase 1: Execute Agent 1 (Gate)

- [ ] **Step 1: Dispatch Agent 1**

Hand it `docs/superpowers/plans/2026-04-27-v0.25-agent1-schema.md`.

Key deliverables:
- `stores_table.dart`: `width_ft REAL nullable`, `depth_ft REAL nullable`
- `fixtures_table.dart`: `zone_id` nullable, `planogram_id TEXT nullable`, `planogram_id_back TEXT nullable`, `wall_adjacent BOOL default false`
- `app_database.dart`: `schemaVersion` 2 → 3
- `stores_dao.dart`: `watchById`, `updateDimensions`
- `fixtures_dao.dart`: `watchStoreLevelByStore`
- `fixture.dart`: @freezed model updated (`zoneId String?`, new fields)
- `floor_builder_provider.dart`: `_rowToFixture`, `_fixtureToCompanion`, `loadFixtures` updated
- Codegen passes, `flutter analyze` clean

- [ ] **Step 2: Verify Agent 1 merged**

```bash
git log --oneline | head -5
flutter build apk --debug 2>&1 | tail -5
```
Expected: Agent 1 commit visible, build succeeds.

---

### Phase 1: Execute Agents 2 and 3 in parallel

- [ ] **Step 3: Dispatch Agent 2 (Store Canvas)**

Hand it `docs/superpowers/plans/2026-04-27-v0.25-agent2-store-canvas.md`.

Key deliverables:
- `zone_map_provider.dart`: `moveZone`, store dimension watch
- `zone_map_painter.dart`: ft grid + store boundary rect
- `zone_map_screen.dart`: setup dialog, whole-zone drag, tap→select/navigate behavior

- [ ] **Step 4: Dispatch Agent 3 (Floor Builder)**

Hand it `docs/superpowers/plans/2026-04-27-v0.25-agent3-floor-builder.md`.

Key deliverables:
- `floor_builder_provider.dart`: `resizeFixture`, `resizeFixtureLocal`, `assignPlanogram`, `toggleWallAdjacent`, planogram map in state
- `builder_canvas_painter.dart`: type-aware edge handles, planogram badges, `resizeHandleRects`
- `floor_builder_screen.dart`: resize gesture, badge tap, wall-adjacent toggle
- `fixture_mini_panel.dart` (new)
- `planogram_picker_sheet.dart` (new)

- [ ] **Step 5: Verify both PRs merged**

```bash
git log --oneline | head -10
flutter build apk --debug 2>&1 | tail -5
```

---

### Phase 2: Execute Agent 4 (Tests)

- [ ] **Step 6: Dispatch Agent 4**

Hand it `docs/superpowers/plans/2026-04-27-v0.25-agent4-tests.md`.

Key deliverables: DAO tests, widget tests for FixtureMiniPanel + PlanogramPickerSheet, integration test.

- [ ] **Step 7: Merge Agent 4 PR**

---

### Final Verification

- [ ] **Step 8: Full build + test**

```bash
flutter build apk --debug 2>&1 | tail -10
flutter test
flutter analyze
```
Expected: all pass, no analyzer warnings.

- [ ] **Step 9: Manual smoke test**

On device/emulator:
1. Open Zone Map → prompted for store dimensions → enter 60 × 40 → confirm
2. Verify ft grid + store boundary visible
3. Tap a zone → selects (banner updates, no sheet)
4. Tap same zone again → navigates to ZoneDetailScreen
5. Back to Zone Map → drag inside a zone → verify whole zone moves → persists on reload
6. Drag a zone vertex → verify vertex reshape still works
7. Open Floor Builder for a zone → select a rack → edge handles appear
8. Drag bottom edge handle → rack depth changes
9. Drag left edge handle on a wall fixture → verify only left/right handles shown
10. Tap grey badge on fixture → PlanogramPickerSheet opens → search → assign
11. Badge turns accent colour with planogram name
12. Tap fixture body → FixtureMiniPanel slides up → VIEW → navigates to PlanogramDetailScreen
13. Add a partition → long-press → "Mark as free-standing" → two badges appear
14. Assign planograms to both badges independently
15. Long-press partition → "Mark as wall-adjacent" → back badge disappears

- [ ] **Step 10: Tag v0.25**

```bash
git tag v0.25.0
git push origin v0.25.0
```
